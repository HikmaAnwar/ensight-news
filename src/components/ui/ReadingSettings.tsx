import { Text } from "@mantine/core";

export function ReadingSettings() {
  return (
    <div className="mt-8">
      <Text className="text-text-muted">
        Placeholder for reading settings (e.g., text size, theme).
      </Text>
    </div>
  );
}
